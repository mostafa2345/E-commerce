services:
  - type: web
    name: e-commerce
    env: node
    buildCommand: npm run install:all && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 18.x
    build:
      cacheVersion: v1
